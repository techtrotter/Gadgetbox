import { FC, ReactNode } from 'react';
import { BkashComponentConfig, BkashSuccessFunction } from './utils';
export declare type BkashButtonProps = {
    onSuccess: BkashSuccessFunction;
    onClose: () => void;
    config: BkashComponentConfig;
    /**
     * Use a custom function to render the error message.
     * @param error The error message.
     */
    renderError?: (error: Error) => JSX.Element | null;
    /**
     * Show a custom loader when bkash is initiating
     */
    loader?: ReactNode;
    /**
     * @default false
     */
    debug?: boolean;
    children: ReactNode;
};
export declare const BkashButton: FC<BkashButtonProps>;
export default BkashButton;
export * from './hooks/useAsync';
export * from './utils';
